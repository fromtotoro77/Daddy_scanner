# Daddy Scanner 개발 계획서

> **daddy 시리즈 5번째 앱** — 스마트폰 브라우저에서 책·문서를 촬영하고 보정하여 PDF로 만드는 웹앱
> 배포 방식: daddy_radio와 동일 (GitHub Pages 정적 호스팅, 웹 주소 접속, APK 없음)

작성일: 2026-08-20

---

## 1. 목표와 핵심 원칙

| 항목 | 내용 |
|---|---|
| 핵심 기능 | **여러 장의 사진 → 한 개의 PDF** (이것이 1순위, OCR은 최후순위) |
| 사용 환경 | 스마트폰 브라우저 (Android Chrome 우선, iOS Safari 대응) |
| 서버 | **없음** — 모든 처리(촬영·보정·PDF 생성)는 브라우저 안에서 완결. 사진이 외부로 전송되지 않음 |
| 배포 | GitHub 저장소 → GitHub Pages (HTTPS 기본 제공 → 카메라 API 사용 가능) |
| 디자인 | **daddy_radio v3.0.0 디자인 시스템 그대로 계승** (Soft Mono 다크 테마) |

---

## 2. 기술 스택 (인기·평판 검증 완료)

| 라이브러리 | 스타 | 역할 |
|---|---|---|
| [jscanify](https://github.com/puffinsoft/jscanify) | 1.8k★ MIT | 문서 테두리 자동 감지 + 원근(사다리꼴) 보정. OpenCV.js 기반, CDN 로드 |
| [OpenCV.js](https://docs.opencv.org/) | (OpenCV 80k★) | jscanify의 엔진. 수동 보정·흑백 이진화·밝기 필터에도 직접 사용 |
| [jsPDF](https://github.com/parallax/jsPDF) | 31k★ MIT | 브라우저에서 이미지 → PDF 생성 |
| (v2 이후) [tesseract.js](https://github.com/naptha/tesseract.js) | 38k★ | OCR — **최후순위**, 이번 버전 범위 밖 |

프레임워크: daddy_radio와 동일하게 **프레임워크 없는 단일 페이지** (index.html + app.css + app.js + sw.js + manifest.json). Tailwind CDN + 커스텀 CSS 병용.

---

## 3. 디자인 시스템 — daddy_radio 계승

daddy_radio `v3.0.0/app.css`의 토큰과 스타일을 그대로 가져온다.

### 3.1 색상 (Soft Mono)

```css
:root {
  --primary-bg:   #0c0c0e;   /* near-black 메인 배경 */
  --surface-bg:   #17171a;   /* 카드/레이어 표면 */
  --brand-primary:#d4d4d8;   /* near-white 포인트 (라이트 징크) */
  --brand-accent: #f43f5e;   /* 단일 레드 강조 — 촬영 셔터·삭제·경고에 사용 */
  --text-main:    #f4f4f5;
  --text-dim:     #8b8b90;
}
```

### 3.2 타이포그래피·질감

- 폰트: Pretendard(본문) / JetBrains Mono(숫자·페이지 카운터) / Outfit(디스플레이) — Google Fonts 로드
- 배경: near-black + 은은한 radial 화이트 글로우 2개 (daddy_radio body 배경 그대로)
- 카드: `--surface-bg` + `border: 1px solid rgba(255,255,255,0.08)` + radius 16~18px + 깊은 그림자
- 버튼: 유리 질감(`rgba(255,255,255,0.05)` + 보더), hover 시 `translateY(-2px)`
- 토스트: daddy_radio `.toast` 스타일 재사용 (블러 + 좌측 브랜드 보더)

### 3.3 헤더 (동일 구조)

```
┌──────────────────────────────┐
│ DADDY SCANNER v1.0    PM 3:42 │   ← 로고 워드마크 + 모노 시계 (blink colon)
└──────────────────────────────┘
```

레이아웃 폭: 모바일 100%, 데스크톱 max-width 480px 중앙 정렬 (`.app-main-width` 동일).

---

## 4. 화면 구성 & 조작 버튼 제안 (요구 4번)

### 제안: "고정 하단 네비" 대신 **화면별 컨텍스트 하단 액션바** ✅ 추천

라디오는 탭 전환형 앱이라 고정 네비가 맞지만, 스캐너는 **촬영 → 페이지 정리 → PDF**의 선형 흐름이다.
고정 네비 3탭을 두면 "지금 뭘 눌러야 하지?"가 생기므로, 각 화면 하단에 그 순간 필요한 버튼만 크게 두는 방식을 추천한다. (카메라 앱들의 표준 UX이기도 함)

```
[화면 1: 촬영]                 [화면 2: 페이지 갤러리]          [화면 3: 페이지 편집]
┌────────────────┐            ┌────────────────┐            ┌────────────────┐
│ DADDY SCANNER  │            │ ← 페이지 (7장)  │            │ ← 3 / 7 페이지  │
│                │            │ ┌──┐ ┌──┐ ┌──┐ │            │                │
│   [카메라       │            │ │1 │ │2 │ │3 │ │            │  [편집 중인     │
│    프리뷰       │            │ └──┘ └──┘ └──┘ │            │   페이지 확대]  │
│    +자동감지    │            │ ┌──┐ ┌──┐ ┌──┐ │            │   ◦───◦        │
│    테두리]      │            │ │4 │ │5 │ │6 │ │            │   │모서리│      │
│                │            │ └──┘ └──┘      │            │   ◦───◦        │
├────────────────┤            ├────────────────┤            ├────────────────┤
│ [갤러리] (셔터) [완료]│       │ [선택] [＋촬영] [PDF 만들기]│  │ [자동보정][수동][필터][회전]│
└────────────────┘            └────────────────┘            └────────────────┘
```

- **화면 1 (촬영)**: 하단 중앙에 대형 원형 셔터(`--brand-accent` 레드 — daddy_radio의 ON AIR 레드 계승). 좌측에 마지막 촬영 썸네일 + 매수 뱃지(누르면 갤러리로), 우측에 "완료". 연속 촬영은 셔터를 계속 누르면 됨 → **한 장이든 여러 장이든 같은 흐름** (요구 1번)
- **화면 2 (갤러리)**: 3열 그리드, 페이지 번호 뱃지(모노 폰트). "선택" 누르면 체크박스 모드 → 체크 후 삭제(레드) (요구 2번). 길게 눌러 드래그하면 순서 변경. 하단 우측 primary 버튼 "PDF 만들기"
- **화면 3 (편집)**: 페이지 1장 확대 + 하단 액션바(자동보정/수동보정/필터/회전). 좌우 스와이프로 페이지 이동

### PDF 완료 시트 (갤러리에서 "PDF 만들기" 후)

바텀시트로: 파일명 입력, 품질 선택(고화질/표준/저용량), **[저장]** + **[공유]** 버튼 (요구 5번).

---

## 5. 기능 명세 (요구사항 매핑)

### 5.1 촬영 — 한 장 / 여러 장 (요구 1)
- `getUserMedia({ video: { facingMode: 'environment' } })` 후면 카메라 프리뷰
- 셔터 1번 = 1페이지 누적. 촬영 즉시 자동 감지·보정 적용 후 갤러리에 추가 (토스트로 "3페이지 추가됨")
- **갤러리 업로드 버튼 병행**: `<input type="file" accept="image/*" multiple>` — 이미 찍어둔 사진 여러 장 한번에 가져오기 (iOS 카메라 제약의 안전망이기도 함)

### 5.2 페이지 갤러리 — 보기/선택/삭제 (요구 2)
- 3열 썸네일 그리드, 페이지 순번 표시
- 선택 모드: 다중 체크 → 일괄 삭제 (삭제 전 확인, 실행 취소 토스트 5초)
- 드래그로 순서 변경 (PDF 페이지 순서 = 갤러리 순서)
- 썸네일 탭 → 화면 3(편집)으로

### 5.3 기울기·원근 보정 — 자동 + 수동 (요구 6) ★핵심
- **자동**: jscanify `findPaperContour()` → `extractPaper()` — 촬영 순간 문서 테두리를 감지해 반듯하게 펴줌. 프리뷰에도 실시간 감지 테두리 오버레이(초록→감지됨)
- **수동**: 자동 감지가 틀렸을 때 — 원본 이미지 위에 **4개 모서리 핸들 드래그** UI(돋보기 확대 보조) → OpenCV.js `warpPerspective`로 재보정. "자동으로 되돌리기" 버튼 포함
- **회전**: 90° 단위 회전 버튼 + 미세 기울기 슬라이더(±10°)

### 5.4 문서용 화질 보정 — 문자 가독성 중심 (요구 7) ★핵심
필터는 촬영 시 기본값 자동 적용 + 편집 화면에서 페이지별 변경:

| 필터 | 구현 | 용도 |
|---|---|---|
| 매직컬러 (기본값) | 대비 스트레칭 + 화이트밸런스 | 그림자 제거, 종이는 하얗게 글자는 진하게 |
| 흑백 문서 | OpenCV `adaptiveThreshold` (적응형 이진화) | 텍스트 최대 선명 + PDF 용량 최소 |
| 그레이스케일 | `cvtColor(GRAY)` | 사진 섞인 페이지 |
| 원본 | 무보정 | 컬러 삽화 페이지 |
| 밝기/대비 수동 | 슬라이더 2개 | 어두운 촬영 구제 (요구 7 "어두운 것을 밝게") |
| 선명하게 | Unsharp mask | 초점이 약간 나간 글자 보강 |

- "전체 페이지에 이 필터 적용" 버튼 제공 (책 한 권 스캔 시 반복 조작 제거)

### 5.5 PDF 생성 (요구 3 — 최우선 기능)
- jsPDF로 A4 세로 기준, 이미지 비율 유지 배치
- 품질 3단계: 고화질(JPEG q0.92) / 표준(q0.8, 장변 2000px) / 저용량(q0.6, 장변 1400px)
- 파일명 기본값: `scan_2026-08-20_1542.pdf` (수정 가능)
- 진행률 표시 (수십 페이지 대비, 페이지별 순차 처리로 메모리 보호)

### 5.6 공유 (요구 5)
- **Web Share API Level 2**: `navigator.share({ files: [pdfFile] })` → 카톡·메일·드라이브 등 OS 공유 시트 (Android Chrome, iOS Safari 15+ 지원)
- 미지원 브라우저: 자동으로 다운로드 버튼만 표시 (`canShare` 체크)

### 5.7 OCR (요구 3 — 최후순위, v2 범위)
- 이번 버전에서 **구현하지 않음**. 추후 tesseract.js(한국어 지원)로 "텍스트 포함 PDF(searchable PDF)" 옵션 추가 여지만 남겨둠 — 구조상 PDF 생성 모듈만 확장하면 되도록 설계

---

## 6. 데이터 보관

- 촬영 페이지는 **IndexedDB**에 Blob으로 저장 (원본 + 보정본 + 보정 파라미터)
- 브라우저를 실수로 닫아도 재접속 시 "이전 작업 7페이지 이어하기?" 복구 제안
- "새 스캔 시작" 시 명시적으로 비움. localStorage에는 설정(기본 필터, 품질)만

---

## 7. 파일 구조 & 배포

```
005_daddy_scanner/
└─ v1.0.0/
   ├─ index.html      # 단일 페이지 (3개 화면 섹션 전환 방식 — daddy_radio와 동일 패턴)
   ├─ app.css         # daddy_radio 디자인 토큰 계승 + 스캐너 전용 스타일
   ├─ app.js          # 카메라/갤러리/보정/PDF 로직
   ├─ sw.js           # 오프라인 캐시 (opencv.js 캐싱이 특히 중요 — 아래 리스크 참고)
   ├─ manifest.json   # PWA — "홈 화면에 추가"하면 앱처럼 실행
   └─ icons/          # daddy 시리즈 아이콘 (모노톤 + 레드 포인트)
```

- 라이브러리는 CDN(jsDelivr) 로드: jscanify, opencv.js, jsPDF — 빌드 과정 없음
- 배포: GitHub 저장소 push → GitHub Pages 활성화 → `https://<계정>.github.io/daddy-scanner/`
- daddy_radio처럼 Cloudflare Worker가 필요한 부분(CORS 우회 등)이 **없음** — 순수 정적 배포로 충분

---

## 8. 개발 단계

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| **1. 골격+촬영** | 디자인 시스템 이식, 화면 3개 골격, 카메라 프리뷰+셔터, 파일 업로드 | 폰에서 여러 장 촬영 → 갤러리에 쌓임 |
| **2. PDF 파이프라인** | 갤러리(삭제·순서변경) + jsPDF 생성 + 다운로드/공유 | 촬영 N장 → PDF 1개 저장·공유 성공 (보정 없이도 최소가치 완성) |
| **3. 자동 보정** | jscanify 통합: 실시간 감지 오버레이 + 촬영 시 자동 펴기 + 매직컬러 기본 필터 | 삐뚤게 찍어도 반듯한 페이지 |
| **4. 수동 보정+필터** | 4모서리 핸들 UI, 회전, 필터 6종, 전체 적용, IndexedDB 복구 | 자동 실패 케이스를 수동으로 구제 |
| **5. 마감** | PWA(sw.js·manifest), 로딩 화면, 실기기 검증(Android Chrome/iOS Safari), GitHub Pages 배포 | 웹 주소 접속만으로 전 기능 동작 |

각 단계마다 실제 스마트폰으로 검증 후 다음 단계 진행. (2단계가 끝나면 이미 "쓸 수 있는 앱"이 되도록 순서 설계)

---

## 9. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| OpenCV.js가 큼 (~8MB) → 첫 로딩 지연 | ① 앱 UI 먼저 띄우고 백그라운드 로드(로드 완료 전엔 자동보정만 비활성) ② sw.js로 영구 캐시 → 두 번째 접속부터 즉시 ③ 로딩 인디케이터 |
| jscanify 감지 실패 (책과 배경 대비 낮음, 펼친 책 곡면) | 수동 4모서리 보정이 1급 기능 (백업이 아니라 동급 UI) |
| iOS Safari 카메라/메모리 제약 | 파일 업로드 경로 병행, 촬영 원본을 즉시 리사이즈(장변 2500px)해 메모리 절약 |
| 수십 페이지 PDF 생성 시 메모리 | 페이지별 순차 인코딩 + 진행률, 품질 프리셋으로 해상도 제한 |
| Web Share API 미지원 환경 | `navigator.canShare` 체크 후 다운로드로 폴백 |
| GitHub Pages 경로 문제 (하위 경로 배포) | 모든 리소스 상대경로, manifest `start_url: "./"` |

---

## 10. v1 범위에서 제외 (추후 후보)

- OCR / 검색 가능한 PDF (tesseract.js) — **요구사항상 최후순위**
- 펼친 책 곡면 평탄화(dewarping), 자동 페이지 분할(펼침면→2쪽)
- 클라우드 저장 연동
