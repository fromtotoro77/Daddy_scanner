# Changelog

이 프로젝트의 모든 변경 사항을 기록합니다. 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따르고,
각 릴리스는 같은 이름의 git 태그(`v1.0.0` 형식)로 표시합니다.

## [1.3.1] - 2026-08-20

### 개선 — 흰 책 겹침 인식 (밝기로 구분 안 되는 경우)
- 겹친 책의 가장자리에 생기는 **가는 그림자 선을 차단벽**으로 사용해 흰 영역을 분리
- 화면 중앙(겨눈 지점)에서 **flood-fill로 퍼져나가 닿는 영역만** 문서로 채택 → 아래 깔린 흰 책·흰 종이는 제외
- 검증: 흰 책 위 흰 책 합성 테스트에서 위쪽 책 모서리를 3px 오차로 정확히 감지, 기존 테스트 전체 통과

## [1.3.0] - 2026-08-20

### 개선 — 감지 엔진 대폭 개편 (실환경 인식률)
- **중앙 우선 원칙**: 화면 중앙(사용자가 겨눈 지점)을 포함하는 후보만 인정 — 책상 위 모니터·키보드 등 다른 밝은 물체에 안내선을 빼앗기지 않음
- **종이색 마스크**: 중앙의 색을 기준으로 "저채도+밝은" 픽셀만 종이 후보로 — 포스트잇·나무 책상·컬러 물체 자동 배제 (1차 시도, 이하 밝기/엣지 폴백 유지)
- **펼침면 = 중앙 고정선 방식으로 재설계**: 책등을 화면 가운데 빨간 고정 점선에 맞추는 UX로 변경.
  좌/우 절반에서 각각 페이지 하나만 찾으면 되므로 책등 그림자 유무와 무관하게 동작.
  감지가 실패해도 고정선 기준으로 확실히 2분할(각 반쪽은 개별 감지·보정)
- 안내 문구: "책등을 빨간 선에 맞추고 촬영하세요"

## [1.2.4] - 2026-08-20

### 수정
- 첫 접속 프리뷰 짤림 근본 수정 — 정사각(2560×2560) 이상값 요청이 가로형 모드로 협상되던 것이 원인.
  화면 방향에 맞는 세로/가로 치수+비율을 명시적으로 요청(camConstraints), 재협상·재시작도 같은 기준 사용
- 화면 회전 시 카메라 스트림을 새 방향에 맞춰 자동 재시작 (가로 펼침면 촬영 대응)

### 추가
- **가로 모드 전용 레이아웃** (vFlat 스타일 참고) — 폰을 눕히면:
  - 촬영: 카메라 미리보기가 화면을 꽉 채우고 불러오기/셔터/페이지 버튼이 우측 세로 열로 이동
  - 편집: 좌측 미리보기 + 우측 필터/슬라이더/버튼 컬럼, 갤러리: 5열
- 진단 도구: 헤더 로고를 빠르게 두 번 탭하면 카메라 해상도/화면 크기 표시 (문제 보고용)

## [1.2.3] - 2026-08-20

### 수정
- 첫 접속 프리뷰 짤림 추가 원인 수정 — 고해상도지만 화면 방향과 어긋난 가로형 프레임이 오는 경우를 기존 저해상도 검사가 통과시킴 → 방향 불일치도 재협상/1회 재시작 조건에 포함
- 화면 미회전 상태로 가로 촬영한 펼침면 감지 — "화면이 회전되지 않아 좌/우 분할을 못 했어요, 자동회전을 켜 주세요" 안내 (잘못 자르는 대신 정확한 안내)

## [1.2.2] - 2026-08-20

### 수정
- 화면 세로 고정 해제(orientation: any) — 펼침면은 폰을 가로로 돌려 찍는 게 정석인데, 설치형 앱이 세로로 고정되어 가로 촬영 시 페이지가 상/하로 보여 분할이 거부되던 문제. 이제 폰을 돌리면 앱도 함께 회전
- 기존 설치 앱은 매니페스트 갱신에 시간이 걸릴 수 있음 — 즉시 적용하려면 홈 화면에서 삭제 후 다시 추가

## [1.2.1] - 2026-08-20

### 수정
- **펼침면 상/하 짤림 해결** — 두 가지 원인 모두 수정
  - 스프링 제본·그림자 같은 어두운 가로 띠가 밝은 영역을 위/아래로 갈라 반쪽만 잡히던 문제: 세로 방향 모폴로지 닫기(healHorizontalBands)로 가로 띠를 메꿈 (세로 책등 분리는 유지)
  - 상/하 두 블롭을 좌/우 페이지로 오인하던 문제: 두 블롭이 좌우로 나란할 때만 펼침면으로 인정
- 펼침면 모드에서 보정 결과가 세로형이면 자르지 않고 한 페이지로 저장 (단일 페이지 오분할 방지)
- 폰 뒤로가기 버튼이 앱을 종료시키던 문제 — 편집→페이지→촬영 순으로 화면 이동, 모달도 닫힘 (촬영 화면에서 한 번 더 누르면 종료)

### 추가
- 펼침면 모드 안내선: 좌/우 페이지 각각 초록 표시 + 책등 분할 경계를 빨간 점선으로 표시, "펼침면 인식됨 — 촬영하면 2페이지로 분할" 안내

## [1.2.0] - 2026-08-20

### 수정
- 배포 후 첫 실행에서 카메라 프리뷰가 저해상도(짤림)로 나오던 문제 — 권한 최초 허용 직후 기본 저해상도 스트림이 오는 기기 대응: 해상도 검사 후 재협상, 실패 시 스트림 1회 재시작

### 추가 — 펼침면 양면 분할
- 촬영 화면 우상단 "펼침면" 토글(설정 유지) — 켜면 펼친 책을 한 번 찍을 때 좌→우 순서로 2페이지 저장
- 좌/우 페이지 블롭을 각각 감지해 **페이지별로 원근+곡면 보정** (책등 쪽 휨도 페이지 단위로 정확)
- 두 페이지가 한 덩어리로 잡히면 통째 보정 후 책등 골짜기(가장 어두운 세로줄) 감지로 절단하는 폴백
- 불러오기(갤러리 사진)에도 동일 적용, 감지 실패 시 한 장으로 저장 + 안내
- 검증: 합성 펼침면 분할 비율 0.522(책등 위치 일치)·전체 커버 확인, 회귀 3종(표 함정·곡면·스모크 14) 통과

## [1.1.2] - 2026-08-20

### 추가
- 시작 스플래시 화면: 앱 아이콘 + Daddy scanner + **to miyeong** 헌정 문구 (라이트 징크+화이트 글로우, 1.5초 후 페이드아웃, 자동 업데이트 새로고침 시엔 짧게)
- 설치형 앱(PWA) OS 스플래시/이름에도 반영 — "Daddy Scanner · to miyeong" (홈 화면 라벨은 Daddy Scanner 유지)

## [1.1.1] - 2026-08-20

### 수정
- 안내선이 페이지 안의 표·글자 블록에 낚이던 문제 해결 — 감지 1순위를 "밝은 종이 영역"으로 변경
  (표/글자는 종이 안쪽 내용이라 종이 바깥 윤곽에 영향 없음 → 제목·페이지 번호까지 포함해 페이지 전체가 잡힘)
- 화면 거의 전체를 덮는 프레임 오감지 방지(상한 97%), 옆 페이지와의 얇은 연결 절단(erode)
- 곡면 페이지의 4점 근사 성공률 향상(근사 강도 단계적 시도)
- 검증: 표 함정 테스트(진한 표+약한 페이지 대비)에서 페이지 모서리 오차 2~4px, 곡면 평탄화 회귀 0.5px, 스모크 14/14

## [1.1.0] - 2026-08-20

### 추가 — 곡면 평탄화 (책 스캔 핵심 기능)
- 촬영 순간 책 페이지의 휨(곡면)까지 자동으로 펴는 곡선 경계 메시 워프
  - 문서 윤곽에서 상·하 테두리를 곡선으로 추출(구간 중앙값+평활, 끝점 모서리 고정) 후 격자 리맵
  - 추가 다운로드 0MB(기존 OpenCV 사용), 처리 ~0.1초 — 딥러닝 방식(30MB·수 초) 대비 압도적 성능
  - 합성 곡면 페이지 검증: 최대 처짐 130px 입력 → 출력 줄 편차 1.0px(0.10%)로 평탄화
- 촬영 안내선에 감지된 상·하 곡선(레드) 표시 — 평탄화될 실제 경계 시각화
- 수동 영역보정: 자동감지 결과를 그대로 적용하면 곡선 유지, 핸들을 직접 움직이면 직선 사각형 적용
- 휨이 미미하면 자동으로 기존 사각 보정 사용(불필요한 리맵 방지), 곡면 워프 실패 시 사각 보정 폴백

## [1.0.4] - 2026-08-20

### 수정
- 카메라 미리보기 잘림 해결 — cover(화면 채우려 상하좌우 잘림) → contain(전체 프레임 표시), 미리보기 = 실제 촬영 범위

## [1.0.3] - 2026-08-20

### 수정
- 새 버전 자동 반영: 새 배포 감지 시 안내 토스트 후 자동 새로고침 (앱 재실행 반복/캐시 삭제 불필요)
- HTML 문서는 HTTP 캐시(GitHub Pages 10분)를 우회해 항상 서버 재검증
- 서비스워커 등록 URL 고정(sw.js) — 릴리스마다 재등록되지 않게 정리

## [1.0.2] - 2026-08-20

### 수정
- 매직컬러 색감 왜곡 수정 — 채널별 독립 스트레칭 → 종이 흰색 기준 화이트밸런스 + 전 채널 공통 대비 곡선
- 갤러리 잔상 버그 수정 — 썸네일 동시 생성 경쟁으로 이미지 URL이 무효화되던 문제(스피너 멈춤 포함)
- 문서 자동 감지 강화 — jscanify 단일 시도 → 다단계 자체 감지(Canny 강/약 + Otsu 폴백 + 볼록 사각형 근사), 기울여 촬영해도 평면 보정 성공률 향상

### 추가/개선
- 촬영 안내선 안정화: 시간 평활(EMA)로 떨림 억제, 모서리 점 표시, "문서 인식됨 — 촬영하세요" 상태 안내 문구
- 좌우 스와이프로 촬영(1) ↔ 페이지(2) 화면 전환

## [1.0.1] - 2026-08-20

### 변경
- 헤더 명칭 변경: DADDY SCANNER → Daddy scanner
- 헤더 시계 제거

### 추가
- 편집 화면 미리보기 핀치 확대/축소(최대 5배)·확대 중 드래그 이동·더블탭 줌 토글·마우스 휠 줌

## [1.0.0] - 2026-08-20

### 첫 릴리스
- 카메라 연속 촬영(후면), 갤러리 사진 다중 불러오기
- 문서 테두리 실시간 감지 오버레이 + 촬영 시 자동 원근 보정 (jscanify/OpenCV.js)
- 수동 영역보정: 4모서리 드래그, 자동감지/전체영역 버튼
- 필터 5종: 매직컬러(기본)·흑백문서(적응형 이진화)·그레이·선명하게·원본
- 밝기/대비 슬라이더, 90° 회전, 전체 페이지 일괄 적용
- 페이지 갤러리: 다중 선택 삭제(5초 실행취소), 순서 이동
- PDF 생성(A4, 고화질/표준/저용량) + Web Share API 공유
- IndexedDB 자동 저장·세션 복원
- PWA: 오프라인 캐시(OpenCV.js 포함), 홈 화면 설치
- Playwright 스모크 테스트 14항목 통과
