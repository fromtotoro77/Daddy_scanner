# Changelog

이 프로젝트의 모든 변경 사항을 기록합니다. 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따르고,
각 릴리스는 같은 이름의 git 태그(`v1.0.0` 형식)로 표시합니다.

## [1.1.0] - 2026-08-20

### 추가 — 곡면 평탄화 (책 스캔 핵심 기능)
- 촬영 순간 책 페이지의 휨(곡면)까지 자동으로 펴는 곡선 경계 메시 워프
  - 문서 윤곽에서 상·하 테두리를 곡선으로 추출(구간 중앙값+평활, 끝점 모서리 고정) 후 격자 리맵
  - 추가 다운로드 0MB(기존 OpenCV 사용), 처리 ~0.1초 — 딥러닝 방식(30MB·수 초) 대비 압도적 성능
  - 합성 곡면 페이지 검증: 최대 처짐 130px 입력 → 출력 줄 편차 1.0px(0.10%)로 평탄화
- 촬영 안내선에 감지된 상·하 곡선(레드) 표시 — 평탄화될 실제 경계 시각화
- 수동 영역보정: 자동감지 결과를 그대로 적용하면 곡선 유지, 핸들을 직접 움직이면 직선 사각형 적용
- 휨이 미미하면 자동으로 기존 사각 보정 사용(불필요한 리맵 방지), 곡면 워프 실패 시 사각 보정 폴백

## [1.0.4] - 2026-08-20

### 수정
- 카메라 미리보기 잘림 해결 — cover(화면 채우려 상하좌우 잘림) → contain(전체 프레임 표시), 미리보기 = 실제 촬영 범위

## [1.0.3] - 2026-08-20

### 수정
- 새 버전 자동 반영: 새 배포 감지 시 안내 토스트 후 자동 새로고침 (앱 재실행 반복/캐시 삭제 불필요)
- HTML 문서는 HTTP 캐시(GitHub Pages 10분)를 우회해 항상 서버 재검증
- 서비스워커 등록 URL 고정(sw.js) — 릴리스마다 재등록되지 않게 정리

## [1.0.2] - 2026-08-20

### 수정
- 매직컬러 색감 왜곡 수정 — 채널별 독립 스트레칭 → 종이 흰색 기준 화이트밸런스 + 전 채널 공통 대비 곡선
- 갤러리 잔상 버그 수정 — 썸네일 동시 생성 경쟁으로 이미지 URL이 무효화되던 문제(스피너 멈춤 포함)
- 문서 자동 감지 강화 — jscanify 단일 시도 → 다단계 자체 감지(Canny 강/약 + Otsu 폴백 + 볼록 사각형 근사), 기울여 촬영해도 평면 보정 성공률 향상

### 추가/개선
- 촬영 안내선 안정화: 시간 평활(EMA)로 떨림 억제, 모서리 점 표시, "문서 인식됨 — 촬영하세요" 상태 안내 문구
- 좌우 스와이프로 촬영(1) ↔ 페이지(2) 화면 전환

## [1.0.1] - 2026-08-20

### 변경
- 헤더 명칭 변경: DADDY SCANNER → Daddy scanner
- 헤더 시계 제거

### 추가
- 편집 화면 미리보기 핀치 확대/축소(최대 5배)·확대 중 드래그 이동·더블탭 줌 토글·마우스 휠 줌

## [1.0.0] - 2026-08-20

### 첫 릴리스
- 카메라 연속 촬영(후면), 갤러리 사진 다중 불러오기
- 문서 테두리 실시간 감지 오버레이 + 촬영 시 자동 원근 보정 (jscanify/OpenCV.js)
- 수동 영역보정: 4모서리 드래그, 자동감지/전체영역 버튼
- 필터 5종: 매직컬러(기본)·흑백문서(적응형 이진화)·그레이·선명하게·원본
- 밝기/대비 슬라이더, 90° 회전, 전체 페이지 일괄 적용
- 페이지 갤러리: 다중 선택 삭제(5초 실행취소), 순서 이동
- PDF 생성(A4, 고화질/표준/저용량) + Web Share API 공유
- IndexedDB 자동 저장·세션 복원
- PWA: 오프라인 캐시(OpenCV.js 포함), 홈 화면 설치
- Playwright 스모크 테스트 14항목 통과
